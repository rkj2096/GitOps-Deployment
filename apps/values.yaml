config:
  spec:
    destination:
      server: https://kubernetes.default.svc
    source:
      repoURL: https://github.com/rkj2096/GitOps-Deployment
      targetRevision: main

applications:
  - name: backstage
    path: backstage
    namespace: backstage
